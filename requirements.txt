# Smart Room Monitor — Python dependencies
# Install with:  pip install -r requirements.txt

opencv-python>=4.9.0      # webcam capture, Haar cascade, drawing
groq>=0.9.0               # Groq Cloud SDK (Vision API)
pygame>=2.5.0             # MP3 audio playback
numpy>=1.26.0             # array ops (OpenCV dependency)
